# Vision Lab 測試紀錄

本文件保存 Vision Lab 每次實驗的可讀摘要。原始逐幀資料、Ground Truth、預測事件與
metrics CSV 放在專案根目錄的 `vision_lab_out/`，並由 `.gitignore` 排除；CSV 是原始證據，
本文件則是應提交到 Git 的研究紀錄。

- 驗收範圍與停止條件：[`PILOT.md`](../PILOT.md)
- 執行與比較方式：[`README.md`](../README.md#vision-lab離線視覺量測)

## 紀錄規則

1. 每批實驗新增一節，不覆寫舊結果。
2. 一律記錄日期、影片來源、run ID、Ground Truth 規則、策略參數與使用的 CSV 檔名。
3. 「逐幀指標」與「事件指標」分開記錄：
   - Precision／Recall／F1 與 frame accuracy 是逐幀指標。
   - 誤報事件、漏失事件、狀態切換與 onset delay 是事件／時序指標。
4. Precision = TP / (TP + FP)，Recall = TP / (TP + FN)，F1 是 Precision 與 Recall 的調和平均。
5. Ground Truth 沒有某狀態的真實樣本時，該狀態的 Recall 與 F1 記為 `N/A`，不得填 0
   或宣稱模型已驗證該狀態。
6. 延遲定義為 `predicted_onset - true_onset`；負值表示模型比人工標記的起點更早觸發。

## 實驗索引

| 實驗 | 日期 | 影片數 | 比較策略 | 狀態 |
| --- | --- | ---: | --- | --- |
| Pilot v1 | 2026-08-28～2026-08-30 | 3 | 單幀 vs 因果 3-of-5 | 完成 |
| V2 個人化 EAR P90 | 2026-08-31 | 4 | 固定 EAR vs 個人化 EAR；單幀與 3-of-5 | 初步完成，待獨立驗證 |
| V2 test4 獨立驗證 | 2026-09-01 | 1 | 凍結的固定 EAR vs P90；單幀與 3-of-5 | 完成 |
| V2 連續 pitch 補償 | 2026-09-01～2026-09-02 | 3 | P90 階梯補償 vs P90 連續補償 | 未通過 test5，不套用正式 app |

---

## Pilot v1：單幀 vs 3-of-5

### 目的

確認使用相同的逐幀特徵時，因果 3-of-5 投票能否減少零記憶單幀分類造成的誤報與狀態抖動，
並量化它增加的偵測延遲。MediaPipe 每支影片只執行一次；兩種策略均從相同的
`frame_features_*.csv` 後處理產生。

### 策略設定

| 策略 | 設定 |
| --- | --- |
| 單幀 | 直接使用當前列 `raw_state`，無計數器、鎖定、遲滯或 cooldown |
| 3-of-5 | 只看當前列與前四列；同一狀態至少 3 票才輸出，視窗不足或無多數時輸出 `normal` |

狀態集合為 `normal`、`eye_closed`、`head_turned`、`posture_down`、`user_missing`。

### 執行版本與環境

- Branch：`codex/vision-lab-pilot-v1`
- Pilot v1 收尾 commit：`5ec01eb`（Document Vision Lab pilot results）
- test1／test2 抽取裝置：Pixel 6a API 31 Android 模擬器，x86_64、CPU MediaPipe
- test1／test2 使用 `VISION_LAB_ASSET` 選片參數與 pubspec asset 登錄執行；此設定納入本次
  Pilot v1 工程收尾。

### 影片與 Ground Truth

| 影片 | 專案檔案 | 拍攝來源 | Run ID | 幀數 | PTS 範圍 | Ground Truth 摘要 |
| --- | --- | --- | --- | ---: | ---: | --- |
| test | `assets/test.mp4` | 自行拍攝；iPhone 16 錄影 | `1787902528095107` | 1707 | 0–56903 ms | normal；20.5–31.9 秒轉頭／分心；32–40 與 42.2–56.4 秒姿態下降 |
| test1 | `assets/test1.mp4` | 自行拍攝；iPhone 16 錄影 | `test1_1788078623363049` | 960 | 0–32001 ms | 0–18.6 秒 normal；18.6–23 秒轉頭／分心；其餘 normal |
| test2 | `assets/test2.mp4` | 自行拍攝；iPhone 16 錄影 | `test2_1788079000832969` | 1013 | 0–33766 ms | 0–4.8 秒 normal；4.8–20 秒低頭／趴下；23.3–32.1 秒離席；其餘 normal |

三支影片皆由研究者自行使用 iPhone 16 拍攝；拍攝日期未記錄。test2 的起身過程
（20–23.3 秒）與人物已回來但臉仍不可見的區段（32.1 秒之後），依「真實人物狀態」標為
`normal`，而不是依模型是否看到臉標記。

本次原始檔案：

- `frame_features_<runId>.csv`
- `ground_truth_<runId>.csv`
- `predicted_events_single_frame_<runId>.csv`
- `predicted_events_3_of_5_<runId>.csv`
- `comparison_metrics_<runId>.csv`

### 各影片結果

| 影片 | 策略 | Frame accuracy | 總誤報事件 | 漏失事件 | 狀態切換 | 已配對／真實事件 | 平均延遲 |
| --- | --- | ---: | ---: | ---: | ---: | ---: | ---: |
| test | 單幀 | 65.9% | 137 | 0 | 171 | 3 / 3 | 990.3 ms |
| test | 3-of-5 | 66.7% | 50 | 0 | 81 | 3 / 3 | 1056.7 ms |
| test1 | 單幀 | 27.1% | 14 | 0 | 27 | 1 / 1 | 101.0 ms |
| test1 | 3-of-5 | 27.2% | 10 | 0 | 19 | 1 / 1 | 168.0 ms |
| test2 | 單幀 | 74.8% | 25 | 0 | 40 | 2 / 2 | -167.0 ms |
| test2 | 3-of-5 | 75.8% | 12 | 0 | 24 | 2 / 2 | -100.5 ms |

### 三支影片合計

Frame accuracy 以 3680 幀加權；平均延遲以 6 個已配對真實事件加權。

| 指標 | 單幀 | 3-of-5 | 變化 |
| --- | ---: | ---: | ---: |
| 加權 frame accuracy | 58.2% | 58.9% | +0.7 個百分點 |
| 總誤報事件 | 176 | 72 | -104（-59.1%） |
| 閉眼誤報事件 | 50 | 39 | -11（-22.0%） |
| 漏失事件 | 0 | 0 | 0 |
| 狀態切換 | 238 | 124 | -114（-47.9%） |
| 平均 onset delay | 456.3 ms | 522.8 ms | +66.5 ms |

### 三支影片合計：逐狀態 Precision／Recall／F1

`Support` 是 Ground Truth 中屬於該狀態的幀數。下表是逐幀指標，不是事件數。

| 狀態 | Support | 單幀 P | 單幀 R | 單幀 F1 | 3-of-5 P | 3-of-5 R | 3-of-5 F1 |
| --- | ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| normal | 1820 | 90.4% | 40.0% | 0.555 | 90.3% | 40.4% | 0.559 |
| eye_closed | 0 | 0.0% | N/A | N/A | 0.0% | N/A | N/A |
| head_turned | 474 | 83.0% | 55.5% | 0.665 | 83.9% | 53.8% | 0.656 |
| posture_down | 1122 | 86.6% | 79.1% | 0.827 | 86.4% | 81.3% | 0.837 |
| user_missing | 264 | 71.4% | 100.0% | 0.833 | 76.3% | 100.0% | 0.866 |

`eye_closed` 的 Ground Truth support 為 0，表示本批資料沒有把任何時間區段標成「持續閉眼」；
因此只能看出模型產生了閉眼誤判，不能由這批影片計算閉眼 Recall 或有效 F1。

### 初步觀察

> 在 3 段影片上，3-of-5 將總誤報事件從 176 次降至 72 次，閉眼誤報事件從 50 次降至
> 39 次，狀態切換從 238 次降至 124 次，漏失維持 0 次；代價是平均偵測延遲增加約
> 0.067 秒。

- 3-of-5 明顯減少事件碎裂與狀態抖動，但加權 frame accuracy 只增加 0.7 個百分點。
- test1 有 700 / 960 幀被單幀規則判為 `eye_closed`。這是持續性的單幀規則偏差，投票無法
  像處理短暫眨眼那樣消除。
- test2 的平均延遲為負值，代表預測事件比人工 Ground Truth 邊界更早開始；這可能同時受到
  粗略人工邊界與模型提早誤觸發影響，不能直接解讀成「偵測更快」。
- 本結果支持「3-of-5 可降低短暫抖動」；尚不足以宣稱整體視覺分類已達高準確率，也不足以
  評估真正閉眼事件的 Recall。

### 決策

Pilot v1 已完成，停止增加報表 UI、5-of-7、遲滯、cooldown 或批次掃描。若另開 v2，第一個
研究問題應限定為持續性 `eye_closed` 誤判，並使用新的校正影片調整規則，再用未參與調參的
影片驗證，避免在同一批資料上調參與宣稱改善。

---

## V2 診斷 1：test1 持續性閉眼誤判

### 日期與問題

- 日期：2026-08-31
- Branch：`codex/vision-lab-v2-eye-calibration`
- 問題：為什麼 test1 有 700 / 960 幀被零記憶規則判為 `eye_closed`？
- 本階段只讀取 Pilot v1 的原始 CSV，沒有修改 FrameClassifier 或正式 app。

### 三支影片的 EAR 概況

`Face + EAR` 是同時有臉且左右 EAR 完整的幀數；閉眼比例以這些幀為分母。

| 影片 | 總幀數 | Face + EAR | `raw_eye_closed` | 閉眼比例 | 平均 EAR 中位數 | 平均 EAR Q25–Q75 | abs(pitch) 中位數／Q95 |
| --- | ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| test | 1707 | 1205 | 318 | 26.4% | 0.1781 | 0.1451–0.2013 | 4.4° / 47.1° |
| test1 | 960 | 960 | 700 | 72.9% | 0.1577 | 0.1471–0.1640 | 2.4° / 4.8° |
| test2 | 1013 | 269 | 147 | 54.6% | 0.1621 | 0.1305–0.1746 | 2.2° / 33.1° |

test2 有大量趴下與離席區段，不能把上表的閉眼比例直接當正常坐姿誤判率；此表只用來比較
輸入特徵的尺度。

### test1 的 Ground Truth 分解

| Ground Truth | 幀數 | 判為 `raw_eye_closed` | 比例 |
| --- | ---: | ---: | ---: |
| normal | 828 | 691 | 83.5% |
| head_turned | 132 | 9 | 6.8% |

test1 全部 960 幀都有臉與左右 EAR，且 abs(pitch) 中位數僅 2.4°、Q95 僅 4.8°，因此持續
閉眼誤判不是缺臉、低頭或 reading-pitch 門檻縮放造成。左右眼 EAR 中位數分別為 0.1670 與
0.1477，存在明顯不對稱；右眼中位數已落在目前規則的低開眼區間。

目前 EAR 會先以 `[0.13, 0.27]` 映射到 `[0, 1]` 的開眼機率，再要求：

```text
min(left_open, right_open) < 0.18
且 average(left_open, right_open) < 0.28
```

在沒有 reading-pitch 縮放且 EAR 尚未被 clamp 的範圍內，這約等於單眼 EAR 小於 0.1552，
同時雙眼平均 EAR 小於 0.1692。test1 的正常坐姿分布大量落在這兩個門檻以下，所以形成
持續性錯誤；3-of-5 只會保留持續錯誤，無法像過濾單次眨眼一樣消除它。

### 結論與停止點

- 原因定位為固定 EAR 映射／門檻不適合 test1 的人物與拍攝條件，而不是 PTS、3-of-5、
  face detection 或 pitch 判斷失敗。
- Pilot v1 三支影片的 `eye_closed` Ground Truth support 為 0，沒有真正閉眼樣本可估計 Recall，
  因此現在不能根據正常幀單方面把門檻調低；那會有漏掉真正閉眼的風險。
- 在取得校正影片以前停止修改 FrameClassifier。

### 下一個最小實驗：閉眼校正影片

使用同一支 iPhone 16、相近距離與光線，固定拍攝一支約 30–45 秒影片，依序包含：

1. 0–8 秒：正常睜眼看前方，標為 `normal`。
2. 8–13 秒：自然眨眼數次，Ground Truth 仍標為 `normal`。
3. 13–18 秒：持續閉眼約 5 秒，標為 `eye_closed`。
4. 18–26 秒：低頭看書且眼睛保持睜開，依本專題的使用情境標為 `normal`。
5. 26 秒到結束：回正並正常睜眼，標為 `normal`。

這支影片只用於選擇閉眼門檻；調整完成後，必須另外錄製至少一支未參與調參的驗證影片，
再報告新的 Precision／Recall／F1，不能直接用校正影片宣稱改善。

### 校正影片實測：test3（含低頭眼鏡遮擋）

- 日期：2026-08-31
- 影片：`assets/test3.mp4`
- 來源：研究者自行使用 iPhone 16 拍攝，配戴眼鏡
- Run ID：`test3_1788164391145578`
- 原始資料：1398 幀，PTS 0–46601 ms，14 欄完整且 timestamp 嚴格遞增
- 本影片是門檻校正／壓力測試資料，不是未參與調參的獨立驗證資料

實際 Ground Truth 依拍攝者提供的時間建立；未指定的 26.4–29 秒轉場標為 `normal`。
Ground Truth 描述的是產品應輸出的使用者狀態，不直接照身體幾何特徵命名，因此正常低頭
看書仍是 `normal`，不是 `posture_down`：

| 時間 | Ground Truth | 說明 |
| ---: | --- | --- |
| 0–21.4 秒 | `normal` | 正常睜眼，包含自然眨眼 |
| 21.4–26.4 秒 | `eye_closed` | 持續閉眼 |
| 26.4–29 秒 | `normal` | 動作轉場 |
| 29–40.5 秒 | `normal` | 正常低頭睜眼看書，眼睛受到眼鏡遮擋 |
| 40.5–46.634 秒 | `normal` | 回正後正常 |

#### 單幀與 3-of-5 事件結果

| 策略 | Frame accuracy | 總誤報事件 | 閉眼誤報事件 | 漏失事件 | 狀態切換 | 已配對／真實事件 | 平均延遲 |
| --- | ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| 單幀 | 60.8% | 19 | 15 | 0 | 40 | 1 / 1 | -2499.0 ms |
| 3-of-5 | 61.0% | 14 | 11 | 0 | 30 | 1 / 1 | -2432.0 ms |

#### 逐狀態 Precision／Recall／F1

| 狀態 | Support | 單幀 P | 單幀 R | 單幀 F1 | 3-of-5 P | 3-of-5 R | 3-of-5 F1 |
| --- | ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| normal | 1248 | 100.0% | 56.1% | 0.719 | 100.0% | 56.3% | 0.721 |
| eye_closed | 150 | 23.8% | 100.0% | 0.384 | 23.9% | 100.0% | 0.386 |
| posture_down | 0 | 0.0% | N/A | N/A | 0.0% | N/A | N/A |

本片沒有 `posture_down`、`head_turned` 或 `user_missing` Ground Truth，因此不能估計這些
狀態的 Recall；其中兩種策略都將 67 個正常閱讀幀誤判為 `posture_down`，所以該狀態
Precision 為 0%。

#### 眼鏡遮擋分段分析

`raw_eye_closed` 是零記憶閉眼旗標；3-of-5 欄是投票後仍輸出 `eye_closed` 的幀數。

| 區段 | 幀數 | `raw_eye_closed` | 3-of-5 閉眼 | 平均 EAR 中位數 | abs(pitch) 中位數 |
| --- | ---: | ---: | ---: | ---: | ---: |
| 0–21.4 秒 normal | 641 | 364（56.8%） | 363（56.6%） | 0.1659 | 1.4° |
| 21.4–26.4 秒真閉眼 | 150 | 150（100.0%） | 150（100.0%） | 0.0626 | 1.5° |
| 26.4–29 秒轉場 normal | 78 | 72（92.3%） | 72（92.3%） | 0.1558 | 0.9° |
| 29–40.5 秒低頭閱讀 normal＋眼鏡遮擋 | 345 | 11（3.2%） | 9（2.6%） | 0.1634 | 33.3° |
| 40.5 秒後 normal | 184 | 34（18.5%） | 34（18.5%） | 0.1765 | 1.3° |

#### 觀察與決策

- 真閉眼 150 幀全部被辨識，閉眼 Recall 為 100%；但正常區段有大量持續性誤判，使閉眼
  Precision 只有約 24%。目前規則偏向「寧可多報、不要漏報」。
- 3-of-5 將閉眼誤報事件從 15 降至 11、狀態切換從 40 降至 30，但逐幀 Precision 幾乎
  不變，證實主要錯誤是持續性門檻偏差，不是短暫眨眼抖動。
- 平均延遲是約 -2.5 秒，原因是閉眼誤判已在真實閉眼前開始，並連續跨入真實閉眼區段；
  負值不代表提早正確預測，而是另一個持續性誤報的訊號。
- 本片的低頭眼鏡遮擋區段只有 3.2% 幀被零記憶閉眼旗標誤判，因此眼鏡遮擋在這支影片中
  不是主要閉眼誤報來源。
- 低頭看書是正常行為，但該區段有 67 / 345 幀輸出 `posture_down`，亦即區段內 19.4%
  的幀是假陽性；該段 abs(pitch) 中位數 33.3°，接近目前固定的 35° 門檻。這是另一個
  正常閱讀姿態誤報問題，不與閉眼門檻混在同一次修改中。
- 下一步可只在這支校正影片的 CSV 上選擇較保守的閉眼候選門檻，不必重跑 MediaPipe；
  確定候選後，再用一支全新影片做最終驗證。

---

## V2 診斷 2：個人化睜眼 EAR P90

### 假設與方法

固定的 `OPEN_EYE_EAR = 0.27` 高於四支影片多數正常睜眼量測值，因此先用既有 CSV 模擬
個人化睜眼基準，再修改 Vision Lab 程式重算一次。校正只使用每支影片前 4 秒、偵測到臉、
左右 EAR 完整且 `abs(pitch) < 15°` 的樣本；左右眼分別取 P90。每支影片都有 119 個有效
校正樣本。前 4 秒在所有 Ground Truth 中皆為 `normal`，但仍屬同批資料內校正，不視為獨立驗證。

選 P90 而不是最大值，是為了避免單一 landmark 離群值。離線推估在 test3 上的結果如下：

| 睜眼基準 | 左／右 EAR | 閉眼 Precision | 閉眼 Recall | 閉眼 F1 | Frame accuracy |
| --- | ---: | ---: | ---: | ---: | ---: |
| 固定 0.27 | 0.2700／0.2700 | 23.8% | 100.0% | 0.384 | 60.8% |
| 前 4 秒最大值 | 0.2171／0.1724 | 44.6% | 100.0% | 0.617 | 81.9% |
| 前 4 秒 P90 | 0.2118／0.1654 | 44.9% | 100.0% | 0.620 | 82.0% |

### 實作後的校正值

使用 `tool/vision_lab_reclassify.dart` 從原始逐幀特徵產生 `frame_features_p90_*.csv`；沒有
重新執行 MediaPipe，也沒有覆寫原始 CSV。同一份 test3 輸入重算兩次後以 `fc.exe /b`
比較為無差異。正式 app 的 `EyeStateDetector` 沒有修改。

| 影片 | 校正樣本 | 左眼 P90 | 右眼 P90 |
| --- | ---: | ---: | ---: |
| test | 119 | 0.248910 | 0.178425 |
| test1 | 119 | 0.195295 | 0.171274 |
| test2 | 119 | 0.197135 | 0.161670 |
| test3 | 119 | 0.211758 | 0.165436 |

### Frame accuracy：固定基準 vs P90

| 影片 | 單幀固定 | 單幀 P90 | 3-of-5 固定 | 3-of-5 P90 |
| --- | ---: | ---: | ---: | ---: |
| test | 65.9% | 67.1% | 66.7% | 68.0% |
| test1 | 27.1% | 80.2% | 27.2% | 80.2% |
| test2 | 74.8% | 80.1% | 75.8% | 80.8% |
| test3 | 60.8% | 82.0% | 61.0% | 82.0% |
| 四支加權 | 58.9% | 76.3% | 59.5% | 76.7% |

### test3 真閉眼結果

test3 是目前唯一含 `eye_closed` Ground Truth 的影片，因此 Recall 只能由這支估計。

| 策略 | 基準 | 閉眼 P | 閉眼 R | 閉眼 F1 | 閉眼誤報事件 | 狀態切換 | 平均延遲 |
| --- | --- | ---: | ---: | ---: | ---: | ---: | ---: |
| 單幀 | 固定 | 23.8% | 100.0% | 0.384 | 15 | 40 | -2499 ms |
| 單幀 | P90 | 44.9% | 100.0% | 0.620 | 10 | 30 | -99 ms |
| 3-of-5 | 固定 | 23.9% | 100.0% | 0.386 | 11 | 30 | -2432 ms |
| 3-of-5 | P90 | 44.9% | 100.0% | 0.620 | 10 | 28 | -32 ms |

### 事件層限制與決策

四支影片合計時，P90 大幅降低錯誤幀的持續時間，但沒有同等幅度降低誤報「事件數」：單幀
閉眼誤報事件只從 65 降至 61；3-of-5 反而從 50 增至 57。test1 原本的長時間連續誤判被
切成多個較短區段，因此 frame accuracy 27.1% → 80.2%，但閉眼誤報事件 14 → 18。這表示
個人化基準解決了大量持續性錯誤，卻尚未解決殘留錯誤的事件碎裂，不能只看 accuracy 宣稱完成。

- 初步資料支持使用「左右眼各自 P90」，成效優於固定值，也略優於直接取最大值。
- test3 的真閉眼 Recall 維持 100%，但只有一支正樣本且參與方案選擇，結果有調參偏差。
- 下一步只錄一支未參與調參的驗證影片：前 4 秒正常睜眼，之後包含自然眨眼、持續閉眼與
  正常低頭閱讀。先凍結 P90 公式，不再用新影片回頭調整參數。

---

## V2 獨立驗證：test4

### 資料與 Ground Truth

- 日期：2026-09-01
- Branch：`codex/vision-lab-v2-validation`
- 影片：`assets/test4.mp4`，研究者自行使用 iPhone 16 拍攝
- Run ID：`test4_1788248409409540`
- 原始資料：1148 幀，PTS 0–38268 ms；估計結束 38301 ms
- 解碼器固定略過 1 個 33 ms sample；其餘輸入 1149、輸出 1148 幀
- P90 校正：前 4 秒 119 幀，左眼 0.206248、右眼 0.171256
- 本片在 P90 公式凍結後才錄製，沒有參與先前的基準選擇

Ground Truth 依拍攝者提供的時間建立。20.2–30.8 秒是正常低頭看書，產品期望狀態仍是
`normal`，所以與後續 normal 合併：

| 時間 | Ground Truth | 說明 |
| ---: | --- | --- |
| 0–13.6 秒 | `normal` | 正常睜眼，包含自然眨眼 |
| 13.6–20.2 秒 | `eye_closed` | 持續閉眼 |
| 20.2–38.301 秒 | `normal` | 20.2–30.8 秒低頭看書，之後回復正常 |

### 固定基準 vs P90

兩組結果都從同一份原始逐幀 CSV 重算，沒有再次執行 MediaPipe。Lab 直接產生的 P90 CSV
與 CLI 重算檔以 `fc.exe /b` 比較無差異。

| 策略 | 基準 | Frame accuracy | 閉眼 P | 閉眼 R | 閉眼 F1 | 閉眼誤報事件 | 狀態切換 | 平均延遲 |
| --- | --- | ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| 單幀 | 固定 0.27 | 36.9% | 21.5% | 100.0% | 0.354 | 10 | 21 | -5467 ms |
| 單幀 | 個人化 P90 | 74.9% | 40.7% | 100.0% | 0.579 | 10 | 22 | -34 ms |
| 3-of-5 | 固定 0.27 | 37.2% | 21.5% | 100.0% | 0.355 | 7 | 15 | -5400 ms |
| 3-of-5 | 個人化 P90 | 74.7% | 40.5% | 99.5% | 0.575 | 9 | 20 | 33 ms |

固定基準的約 -5.4 秒延遲不是良好的提早偵測，而是模型在真實閉眼前已持續誤判；P90 將
onset 誤差收斂到單幀 -34 ms、3-of-5 +33 ms。

### 各區段的單幀閉眼輸出

| 區段 | 幀數 | 固定 0.27 | 個人化 P90 | P90 平均 EAR 中位數 | abs(pitch) 中位數 |
| --- | ---: | ---: | ---: | ---: | ---: |
| 0–13.6 秒 normal | 407 | 234（57.5%） | 43（10.6%） | 0.1681 | 3.8° |
| 13.6–20.2 秒真閉眼 | 198 | 198（100.0%） | 198（100.0%） | 0.0723 | 3.8° |
| 20.2–30.8 秒低頭閱讀 normal | 318 | 291（91.5%） | 182（57.2%） | 0.1398 | 19.1° |
| 30.8 秒後 normal | 225 | 199（88.4%） | 63（28.0%） | 0.1568 | 7.2° |

### 結論與停止點

- 獨立 test4 支持個人化 P90：單幀 accuracy 增加 38.0 個百分點，閉眼 Precision 約翻倍，
  真閉眼 198 幀全數保留。
- P90 尚未解決閉眼誤報；Precision 仍只有 40.7%，尤其正常低頭閱讀仍有 57.2% 幀誤判。
- 事件數沒有同步改善：單幀閉眼誤報事件維持 10，3-of-5 從 7 增至 9，仍有事件碎裂。
- test4 是獨立驗證資料，不用來回頭修改 P90。下一輪若繼續，應另開問題分析「低頭閱讀時
  EAR 與 pitch 的交互影響」，並另外保留新的驗證影片。

---

## V2 診斷 3：連續 pitch 門檻補償

### 假設與凍結規則

- 日期：2026-09-01
- Branch：`codex/vision-lab-v2-validation`
- 目的：檢查低頭閱讀時 EAR 下降，是否可由連續的 pitch 補償降低閉眼誤判。
- 開發資料：test3；數值檢查：test4。
- 原始 CSV 未修改，MediaPipe 未重跑；新增輸出使用 `frame_features_pitch_aware_*.csv`。
- 正式 app 的 `EyeStateDetector` 未修改；此策略只以 `--mode=p90-pitch-aware` 在 Vision Lab
  離線重算時啟用，既有 `--mode=p90` 行為保持不變。

test3 正坐 normal 的 `abs(pitch)` P90 為 3.1°，低頭閱讀中位數為 33.3°。因此將開始補償
角度定為 5°；完整補償角度 25° 與縮放值 0.6 沿用既有規則，沒有用 test4 搜尋參數：

```text
progress = clamp((abs(pitch) - 5) / (25 - 5), 0, 1)
threshold_scale = 1.0 - 0.4 * progress
```

P90、固定閉眼端點 0.13、單眼／平均 openness 門檻 0.18／0.28 均未改變。新策略只是把
原本 `pitch < 25° → 1.0`、`pitch >= 25° → 0.6` 的階梯切換改為連續過渡。

### 資料限制

pitch 與 EAR 的關係沒有在兩片中保持一致：test3 normal 的線性相關很弱且略為正向
（`r = 0.111`，每度平均 EAR 約 `+0.00020`），test4 normal 則為中度負向
（`r = -0.405`，每度約 `-0.00131`）。因此本輪沒有把 test3 擬合出的 EAR–pitch 斜率直接
套到 test4，而只測試較保守的連續化既有補償。test4 已曾用來發現低頭誤報問題，所以本輪
只能視為保留參數後的數值檢查；仍需全新的 test5 才能作最終獨立驗證。

### test3 開發資料 sanity check

| 策略 | 時序 | Frame accuracy | 閉眼 P | 閉眼 R | 閉眼 F1 | 閉眼誤報事件 | 狀態切換 | 平均延遲 |
| --- | --- | ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| P90 階梯補償 | 單幀 | 82.0% | 44.9% | 100.0% | 0.620 | 10 | 30 | -99 ms |
| P90 連續補償 | 單幀 | 82.1% | 45.0% | 100.0% | 0.621 | 10 | 30 | -99 ms |
| P90 階梯補償 | 3-of-5 | 82.0% | 44.9% | 100.0% | 0.620 | 10 | 28 | -32 ms |
| P90 連續補償 | 3-of-5 | 82.1% | 45.0% | 100.0% | 0.621 | 10 | 28 | -32 ms |

test3 的閱讀 pitch 多數已超過原本 25° 完整補償點，所以連續化幾乎不改變結果；只有一個
normal 轉場幀由閉眼改為正常。這確認新模式沒有破壞 test3 的 150 / 150 真閉眼辨識。

### test4 數值檢查

| 策略 | 時序 | Frame accuracy | 閉眼 P | 閉眼 R | 閉眼 F1 | 閉眼誤報事件 | 狀態切換 | 平均延遲 |
| --- | --- | ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| P90 階梯補償 | 單幀 | 74.9% | 40.7% | 100.0% | 0.579 | 10 | 22 | -34 ms |
| P90 連續補償 | 單幀 | 79.0% | 45.1% | 100.0% | 0.622 | 13 | 28 | -34 ms |
| P90 階梯補償 | 3-of-5 | 74.7% | 40.5% | 99.5% | 0.575 | 9 | 20 | 33 ms |
| P90 連續補償 | 3-of-5 | 78.7% | 44.8% | 99.5% | 0.618 | 12 | 26 | 33 ms |

test4 的正常低頭閱讀區段共有 318 幀：單幀閉眼誤判由 182 幀（57.2%）降至 138 幀
（43.4%），少 44 幀、相對減少 24.2%；真正閉眼維持 198 / 198。正坐前段維持 43 個
閉眼誤判幀，回正後則由 63 降至 60。

### 結論與停止點

- 連續 pitch 補償支持「低頭角度應參與閉眼門檻」的方向：test4 閱讀誤判幀與閉眼 F1 都有
  改善，且沒有犧牲單幀真閉眼 Recall。
- 它沒有解決問題：低頭閱讀仍有 43.4% 幀誤判閉眼。
- 誤報事件由單幀 10 → 13、3-of-5 9 → 12，狀態切換也增加，表示原本較長的錯誤被切成
  更多短事件。只看 frame accuracy 會高估改善。
- 目前保留此策略為 Vision Lab 可選實驗模式，不套用正式 app。下一步應錄製全新的 test5，
  包含正坐睜眼、持續閉眼、不同幅度低頭閱讀與回正；若 test5 仍能降低閱讀誤判且維持閉眼
  Recall，再決定是否加入 live app。若事件碎裂仍增加，應研究事件持續時間，而不是繼續在
  test4 上調 pitch 參數。

### 可重現性

同一份 test4 原始 CSV 以 `--mode=p90-pitch-aware` 重算兩次，使用 `fc.exe /b` 比較為無差異。

---

## V2 獨立確認：test5

### 資料與 Ground Truth

- 日期：2026-09-02
- Branch：`codex/vision-lab-v2-validation`
- 影片：`assets/test5.mp4`，研究者自行拍攝
- Run ID：`test5_1788332332176762`
- 原始資料：1799 幀，PTS 0–59965 ms；估計結束 59998 ms
- 解碼器固定略過 1 個約 28 ms 的 sample；輸入 1800、輸出 1799 幀
- P90 校正：前 4 秒 120 幀，左眼 0.216559、右眼 0.173397
- 本片在 P90＋連續 pitch 補償公式凍結後錄製，沒有用來調整參數

Ground Truth 依拍攝者提供的時間建立；低頭看書依產品語意標為 `normal`：

| 時間 | Ground Truth | 說明 |
| ---: | --- | --- |
| 0–7.2 秒 | `normal` | 正坐正常睜眼 |
| 7.2–14.1 秒 | `eye_closed` | 持續閉眼 |
| 14.1–23.5 秒 | `normal` | 恢復正常 |
| 23.5–52.8 秒 | `normal` | 低頭看書 |
| 52.8 秒～結束 | `normal` | 回正後正常 |

### 凍結策略比較

P90 階梯補償與 P90 連續補償皆從同一份原始 CSV 重算，沒有重新執行 MediaPipe。
兩份重算後的逐幀 CSV 使用 `fc.exe /b` 比較為無差異，因此本片上連續補償沒有改變任何
一幀的分類結果。

| 策略 | 時序 | Frame accuracy | 閉眼 P | 閉眼 R | 閉眼 F1 | 閉眼誤報事件 | 狀態切換 | 平均延遲 |
| --- | --- | ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| P90 階梯補償 | 單幀 | 49.4% | 32.0% | 99.5% | 0.484 | 6 | 16 | 61 ms |
| P90 連續補償 | 單幀 | 49.4% | 32.0% | 99.5% | 0.484 | 6 | 16 | 61 ms |
| P90 階梯補償 | 3-of-5 | 49.1% | 31.7% | 98.6% | 0.479 | 6 | 16 | 128 ms |
| P90 連續補償 | 3-of-5 | 49.1% | 31.7% | 98.6% | 0.479 | 6 | 16 | 128 ms |

### 分段診斷

下表使用零記憶單幀輸出。`raw_eye_closed` 可與最終 `raw_state` 不同，因為最終狀態優先序
會讓 `posture_down` 蓋過 `eye_closed`。

| 區段 | 幀數 | `raw_eye_closed` | 最終 normal | 最終 eye_closed | 最終 posture_down | 平均 EAR 中位數 | abs(pitch) 中位數 |
| --- | ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| 0–7.2 秒 normal | 216 | 0（0.0%） | 216 | 0 | 0 | 0.1860 | 0.5° |
| 7.2–14.1 秒真閉眼 | 207 | 206（99.5%） | 1 | 206 | 0 | 0.0868 | 0.8° |
| 14.1–23.5 秒 normal | 282 | 40（14.2%） | 242 | 40 | 0 | 0.1738 | 2.4° |
| 23.5–52.8 秒閱讀 normal | 879 | 398（45.3%） | 9 | 398 | 472 | 0.1655 | 37.7° |
| 52.8 秒後 normal | 215 | 0（0.0%） | 215 | 0 | 0 | 0.1758 | 1.4° |

閱讀區段的 pitch Q25–Q75 為 17.6°–39.6°，其中 396 幀低於 25°；即使如此，連續補償
沒有讓任何一幀跨過閉眼判定邊界。更嚴重的是 472 / 879 閱讀幀因目前單幀規則
`abs(pitch) >= 35°` 被輸出為 `posture_down`。因此閱讀區段只有 9 / 879 幀得到產品期望的
`normal`，整體 frame accuracy 才降到約 49%。

### 結論與停止點

- test5 不支持目前的連續 pitch 門檻補償：相較既有 P90 階梯規則，逐幀、事件與延遲全部
  無改善，故不套用正式 app。
- 真閉眼仍容易辨識：單幀為 206 / 207，Recall 99.5%；主要問題仍是假陽性，而不是漏掉
  持續閉眼。
- 正常閱讀同時暴露兩個獨立問題：398 個閉眼誤判幀，以及 472 個 `posture_down` 誤判幀。
  單靠調整閉眼門檻無法解決產品語意上的「正常低頭閱讀」。
- 停止在 test5 上繼續調參。下一個問題應先重新定義閱讀與趴下的區別，避免只用固定 pitch
  35° 判 `posture_down`；提出候選規則後必須再用新影片驗證，而不能用 test5 宣稱成效。

### 可重現性

Lab 直接產生的 P90 CSV、CLI `--mode=p90` 重算檔，以及 `--mode=p90-pitch-aware` 重算檔
逐列相同；三者皆使用相同的 1799 列原始特徵。

---

## V2 資料層：Pose context 原始欄位

### 目的與範圍

- 日期：2026-09-02
- Branch：`codex/vision-lab-v2-pose-context`
- 目的：讓後處理能研究「低頭閱讀／打瞌睡／趴下」的差異，不再只依賴 `pitch`。
- 本輪只擴充逐幀原始資料；沒有新增閱讀、疲勞或趴下規則，也沒有改變正式 app 行為。
- 現行 CSV 由 14 欄擴充為 43 欄；舊 14 欄 CSV 仍可由同一個 CLI 重算。

新增資料包含影像尺寸，以及 Pose 的 nose、雙眼、雙耳、雙肩、雙髖之原始像素座標與
MediaPipe visibility。比例、角度、速度、穩定時間和事件仍由後處理計算，不寫回原始欄位。

### test5 端到端驗收

| 項目 | 結果 |
| --- | --- |
| 完整 Run ID | `test5_1788333737859222`、`test5_1788334581949564` |
| 每次列數／欄數 | 1799 列／43 欄 |
| PTS | 0–59965 ms，嚴格遞增 |
| 影像尺寸 | 360 × 640 |
| Pose detected | 1799 / 1799 |
| nose、雙肩、雙髖座標填值 | 各 1799 / 1799 |
| 同片兩次完整 CSV | `fc.exe /b` 無差異 |
| 同一 CSV 本機 P90 重算兩次 | `fc.exe /b` 無差異 |

test5 中 nose 與雙肩 visibility 約為 0.99–1.00；雙髖雖每幀有座標，但 visibility 只有
約 0.00–0.06，部分座標也落在影像邊界外。這表示鏡頭沒有可靠拍到髖部，後處理必須先看
visibility，不能因為座標欄非空就採用。這也支持保留 raw confidence、不要在抽取階段先把
低品質點轉成姿勢結論。

### 正式姿勢邏輯離線回放

使用同一份 test5 CSV，以正式 App 的 `PostureDownDetector` 逐幀回放。該 detector 使用
鼻子、雙肩、影像尺寸、visibility、校正基準、One Euro filter、下降動態和 2-of-3 確認，
不使用髖部。眼睛、轉頭與離席仍保留原本 P90 單幀 baseline，避免把這個實驗誤稱為完整
live app 回放。

| 姿勢規則 | 時序 | Frame accuracy | 趴下判定幀 | 閉眼誤報事件 | 狀態切換 | 平均延遲 |
| --- | --- | ---: | ---: | ---: | ---: | ---: |
| Pilot `abs(pitch) >= 35°` | 單幀 | 49.4% | 472 | 6 | 16 | 61 ms |
| 正式 `PostureDownDetector` | 單幀 | 75.6% | 0 | 6 | 14 | 61 ms |
| Pilot `abs(pitch) >= 35°` | 3-of-5 | 49.1% | 472（投票前） | 6 | 16 | 128 ms |
| 正式 `PostureDownDetector` | 3-of-5 | 75.4% | 0（投票前） | 6 | 14 | 128 ms |

正式姿勢邏輯將閱讀區段的 472 個 `posture_down` 誤判全部移除；879 個閱讀幀中，481 幀回到
`normal`，其餘 398 幀仍被既有眼睛 baseline 判成 `eye_closed`。因此本片的姿勢誤判已明顯
改善，剩餘主要問題是低頭閱讀時的眼睛誤報。

test5 沒有真正趴下事件，所以此結果只能證明姿勢 false positive 降低，不能估計正式姿勢
邏輯的 Recall。下一支驗證影片必須同時包含正常閱讀和真正趴下，才可決定是否以正式姿勢
回放取代 Vision Lab 的粗略姿勢 baseline。

### 操作限制

同一個模擬器 App 程序連續跑第二次 test5 時，原生端在 1769 / 1799 幀後被 Android 系統
終止，留下 `.csv.native`；重新啟動乾淨程序後即可完整跑完，且與第一次逐列相同。本輪不把
長片重跑的資源釋放問題擴大成新功能；在修正前，做可重現性驗收時每次長片 run 之間應重啟
Vision Lab。

### 下一步與停止點

- 錄製或重跑一支新版 43 欄影片，內容同時包含正常低頭閱讀與真正趴下，驗證正式姿勢邏輯
  的 Precision 與 Recall。
- test5 已支持「正式姿勢邏輯可降低正常閱讀的趴下誤報」，但不支持任何趴下 Recall 結論。
- 眼睛誤報應獨立處理，不再用姿勢規則的改善掩蓋 398 個閱讀閉眼誤判幀。

---

## V2 探索分析：移除固定閉眼端點

### 問題與資料角色

- 日期：2026-09-02
- Branch：`codex/vision-lab-v2-pose-context`
- 使用 test3、test4、test5 的既有逐幀 EAR 與 Ground Truth；MediaPipe 未重跑。
- 本節是用來提出候選規則的探索分析，三支影片都已參與選擇門檻，不能再當獨立驗證集。
- 指標只評估 `eye_closed`／非 `eye_closed` 二元任務，姿勢與離席不列入本表。

現行算法先用左右眼前 4 秒 P90 當個人睜眼基準，但另一端固定使用 `EAR = 0.13` 當完全
閉眼，再把 EAR 映射成 openness。資料顯示固定端點會丟失有用差異：test5 右眼在低頭閱讀
時 EAR 中位數約 0.098，真正閉眼約 0.055；兩者原本可分，但都低於 0.13，因此都會被截成
openness 0。

三片的 P90 約為左眼 0.206–0.217、右眼 0.165–0.173。P90 取分布高端，不會被少數眨眼
拉低；改用單一最大值反而更容易受雜訊影響，也會讓大部分幀的相對 openness 更低。

### 候選規則

移除固定 0.13 閉眼端點，保留個人 P90，直接計算：

```text
relative_ear = ((ear_l / p90_l) + (ear_r / p90_r)) / 2
closed_candidate = relative_ear < 0.50
```

`0.50` 是在 test3–test5 上探索得到的候選值，不是文獻常數，也不是已驗證的正式參數。

### 三片合併結果

| 策略 | Frame accuracy | Precision | Recall | F1 | 誤報幀 | 漏失幀 | 真閉眼事件 |
| --- | ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| 現行 P90＋固定 0.13 端點 | 79.0% | 37.8% | 99.8% | 54.9% | 910 | 1 | 3 / 3 |
| `relative_ear < 0.50` | 97.6% | 84.4% | 99.3% | 91.2% | 102 | 4 | 3 / 3 |
| 上列＋連續 300 ms | 99.1% | 99.6% | 93.0% | 96.2% | 2 | 39 | 3 / 3 |

300 ms 的 frame Recall 較低，是因為每個事件開頭的確認期間會被計為漏失幀；三段長閉眼事件
仍全數配對成功。三片的首次偵測延遲約 268、300、395 ms。誤報事件為 test3 0、test4 0、
test5 1；test5 該事件只輸出 25130–25163 ms 的一幀。

各片單獨使用 `relative_ear < 0.50` 的單幀結果：

| 影片 | Accuracy | Precision | Recall | F1 | 誤報幀 | 漏失幀 |
| --- | ---: | ---: | ---: | ---: | ---: | ---: |
| test3 | 96.9% | 77.7% | 100.0% | 87.5% | 43 | 0 |
| test4 | 97.0% | 86.0% | 99.0% | 92.0% | 32 | 2 |
| test5 | 98.4% | 88.4% | 99.0% | 93.4% | 27 | 2 |

### 結論與下一步

- 目前資料支持「固定閉眼端點 0.13 不適合這位使用者／拍攝條件」，不支持「P90 被眨眼
  污染」。
- 相對 EAR 比例比現行 openness 映射更能保留低頭閱讀與真正閉眼之間的差異。
- 300 ms 持續時間可以排除資料中的大部分眨眼／短暫遮擋，代價是約 0.3–0.4 秒延遲。
- 暫不改正式 App。先把候選規則凍結為 `relative_ear < 0.50`＋300 ms，再用全新的 test6
  驗證；test6 必須包含眨眼、瞇眼、戴眼鏡閱讀、閱讀中眨眼和持續閉眼。
- 若要精確比較 live app，CSV 還需要記錄原生端融合 blendshape 與 landmark 後的左右眼
  openness；目前本節只分析 raw EAR。

---

## V2 獨立驗證：test6 相對 EAR

### 資料與凍結規則

- 日期：2026-09-02
- Branch：`codex/vision-lab-v2-pose-context`
- 影片：`assets/test6.mp4`，研究者自行拍攝
- Run ID：`test6_1788340869592775`
- 原始資料：2264 幀、43 欄，PTS 0–75470 ms；估計結束 75503 ms
- P90：左眼 0.222991、右眼 0.173082，119 個校正樣本
- 驗證前凍結：`relative_ear < 0.50` 且連續 300 ms；沒有用 test6 改門檻或時間

Ground Truth：

| 時間 | Ground Truth | 說明 |
| ---: | --- | --- |
| 0–14.9 秒 | `normal` | 正坐睜眼 |
| 14.9–25.9 秒 | `eye_closed` | 正坐持續閉眼 |
| 25.9–36.3 秒 | `normal` | 回正後進入低頭閱讀 |
| 36.3–58.0 秒 | `eye_closed` | 維持低頭姿勢閉眼／低頭睡覺 |
| 58.0 秒～結束 | `normal` | 低頭閱讀後回正 |

### 獨立結果

下表只評估 `eye_closed`／非 `eye_closed` 二元 frame 指標：

| 策略 | Accuracy | Precision | Recall | F1 | TP 幀 | FP 幀 | FN 幀 |
| --- | ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| test6 原有 P90 規則，單幀 | 67.0% | 73.5% | 37.3% | 49.5% | 366 | 132 | 615 |
| test6 原有 P90 規則，3-of-5 | 66.9% | 73.4% | 36.9% | 49.1% | 362 | 131 | 619 |
| 凍結 `relative_ear < 0.50`，單幀 | 69.8% | 88.6% | 34.9% | 50.0% | 342 | 44 | 639 |
| 凍結相對 EAR＋300 ms | 70.4% | 97.6% | 32.5% | 48.8% | 319 | 8 | 662 |

凍結相對 EAR＋300 ms 產生 2 個誤報事件、漏失 1 / 2 個真正閉眼事件；配對成功的是正坐
閉眼，偵測延遲 366 ms。低頭閉眼事件完全沒有輸出。

分段結果使用凍結相對 EAR＋300 ms：

| 區段 | 幀數 | Face detected | 預測閉眼幀 | 結果 |
| --- | ---: | ---: | ---: | --- |
| 0–14.9 秒正坐 normal | 446 | 446 | 3 | 大致正確 |
| 14.9–25.9 秒正坐閉眼 | 330 | 330 | 319 | 抓到；延遲約 366 ms |
| 25.9–27.0 秒 normal | 33 | 33 | 0 | 正確 |
| 27.0–36.3 秒低頭閱讀 | 279 | 279 | 5 | 大致正確 |
| 36.3–58.0 秒低頭閉眼 | 651 | 217 | 0 | 完全漏失 |
| 58.0–68.1 秒低頭閱讀 | 303 | 303 | 0 | 正確 |
| 68.1 秒後 normal | 222 | 222 | 0 | 正確 |

低頭閉眼時只有 217 / 651 幀仍偵測得到臉；其 face-visible 幀的 relative EAR P10／P50／P90
為 0.517／0.649／0.752，與兩段低頭閱讀高度重疊，沒有低於凍結的 0.50 並連續 300 ms。
因此失敗不只是 face missing：現有 raw EAR 在眼鏡＋低頭角度下也沒有可靠呈現閉眼。

正式 `PostureDownDetector` 回放在 test6 判定 0 個 posture-down 幀；它成功避免把低頭閱讀
誤判趴下，但同時也無法用穩定的肩膀／鼻子幾何區分「維持低頭閱讀姿勢但閉眼睡覺」。

### 結論與停止點

- 凍結候選規則通過「正坐閉眼」與「低頭睜眼」兩部分，但未通過最重要的低頭閉眼驗證，
  所以不能套入正式 App 作為完整解法。
- 不因 test6 失敗而回頭調整 0.50 或 300 ms；test6 保留為獨立失敗證據。
- 下一個最小診斷是把原生端已計算的 blendshape＋landmark 融合後左右眼 openness 寫入 CSV，
  用同一支 test6 檢查正式眼部 feature 是否仍有 reading／closed 可分性。
- 若融合 openness 也重疊，單靠靜態眼部與目前 Pose 幾何不足；需研究臉消失前後的頭部動態、
  更適合低頭角度的眼部模型／相機位置，不能只再掃 EAR 門檻。

### 產品層 normal／sleeping 正式回放

依產品語意重新評估：36.3–58.0 秒不要求一定輸出 `eye_closed`；正式 evaluator 的任何 cause
只要最終 `CompanionStatus.sleeping` 都算成功。為了精確回放，CSV schema 增加原生端已計算
的 `eye_open_l`、`eye_open_r` 與 `head_offset_calibrating`，形成 46 欄；正式 App 行為未修改。

- 新 Run ID：`test6_1788355603679529`
- 2264 幀，PTS 0–75470 ms
- 1830 幀有融合 eye openness，與 face detected 數相同
- Ground Truth：0–14.9 normal、14.9–25.9 sleeping、25.9–36.3 normal、
  36.3–58.0 sleeping、58.0 秒後 normal

| 指標 | 結果 |
| --- | ---: |
| Binary frame accuracy | 57.5% |
| Sleeping Precision | 68.0% |
| Sleeping Recall | 3.5% |
| Sleeping F1 | 6.6% |
| TP／FP／FN 幀 | 34／16／947 |
| 配對睡眠事件 | 1 / 2 |
| 誤報事件／漏失事件 | 5／1 |
| 已配對 onset delay | 101 ms |

分段正式 status：

| 區段 | 幀數 | sleeping 幀 | 主要非 sleeping status |
| --- | ---: | ---: | --- |
| 0–14.9 秒 normal | 446 | 0 | normal 367、fatigue 67、attention 12 |
| 14.9–25.9 秒正坐閉眼 | 330 | 0 | fatigue 330 |
| 27.0–36.3 秒低頭閱讀 | 279 | 12 | normal 241、fatigue 25 |
| 36.3–58.0 秒低頭睡覺 | 651 | 34 | normal 598、fatigue 10、distracted 9 |
| 58.0–68.1 秒低頭閱讀 | 303 | 4 | normal 288、fatigue 11 |
| 68.1 秒後 normal | 222 | 0 | normal 174、fatigue 38、attention 10 |

正式 evaluator 未通過這次 normal／sleeping 驗收。正坐閉眼在目前產品狀態機被設計為
`fatigue` 而非 `sleeping`；低頭睡覺則因 face 僅部分可見且姿勢幾何與穩定閱讀相近，多數
被閱讀保護留在 normal。即使把 `fatigue` 也視為睡眠，低頭睡覺仍只有 44 / 651 幀命中，
不足以宣稱成功。本輪只加入可重現回放與原始特徵，不修改正式狀態邏輯。

### V2 診斷：test6 頭部動態與低頭深度

- 日期：2026-09-03
- 目的：檢查「閱讀時頭部較穩定，打瞌睡時會下垂／回正」是否能補足低頭閉眼時 EAR 與
  face detection 失效的問題。
- 資料：同一份 46 欄 `test6_1788355603679529` CSV；沒有重跑 MediaPipe，也沒有修改正式 App。
- 方法：各區段只用 face 可見幀計算 `abs(pitch)`；Pose 幀另以
  `nose_y_ratio = pose_nose_y / image_height` 計算鼻子高度，並比較相鄰幀速度。

| 區段 | Face 可見率 | Pitch 中位數／IQR | Pitch 速度 P90 | Nose Y 中位數／IQR | Nose Y 速度 P90 |
| --- | ---: | ---: | ---: | ---: | ---: |
| 27.0–36.3 秒閱讀 | 100.0% | 38.41°／1.20° | 7.35°/s | 0.5006／0.0041 | 0.0673/s |
| 36.3–58.0 秒低頭睡覺 | 33.3% | 39.41°／3.18° | 16.64°/s | 0.5451／0.0156 | 0.1400/s |
| 58.0–68.1 秒閱讀 | 100.0% | 36.90°／0.63° | 5.03°/s | 0.5053／0.0033 | 0.0465/s |

本片支持頭部動態假設：睡覺段的 pitch 速度 P90 約為兩段閱讀的 2.3–3.3 倍，Pose 鼻子速度
P90 約為 2.1–3.0 倍。不過更明顯的訊號不是單純「反覆點頭」，而是睡覺段鼻子位置更低：
兩段閱讀的 Nose Y 中位數約 0.50，睡覺為 0.5451；睡覺段也只有三分之一幀仍看得到臉。

為估計可分性，使用本片資料事後建立以下**診斷候選**，只評估 27.0–68.1 秒低頭區段：

```text
nose_y_ratio - 前 5 個合格 Pose baseline 的中位數 >= 0.13
再套 3-of-5
```

test6 baseline 為 0.38369；候選得到 TP 591、FP 0、FN 60、TN 582，Accuracy 95.1%、
Precision 100.0%、Recall 90.8%、F1 95.2%，首次觸發相對 36.3 秒真值延遲 1035 ms。
同一相對門檻套在 test5 的 879 幀低頭閱讀，得到 0 個誤報幀。

這不是獨立驗證：`0.13` 是看過 test6 後才選出的門檻，test5 也只有 negative 閱讀樣本，
因此不能據此修改正式狀態機或宣稱泛化成功。目前只能下結論：相對 Pose 低頭深度＋短窗時序
在現有資料上比單幀 EAR 更值得驗證。下一步應凍結此候選，再用一支新的、同時包含低頭閱讀
與低頭睡覺的影片做獨立測試；新影片通過前不調參。

### V2 獨立驗證：test7 Pose 低頭深度候選

- 日期：2026-09-03
- 影片：`assets/test7.mp4`，研究者自行拍攝
- Run ID：`test7_1788425425933008`
- 原始資料：3386 幀，PTS 0–112873 ms，46 欄；解碼器記錄 1 個 33 ms dropped sample
- 驗證前凍結：`nose_y_ratio - baseline >= 0.13`，再套因果 3-of-5；沒有用 test7 調門檻
- baseline：前 5 個 `face + pose` 且 `abs(pitch) <= 25°` 樣本的 Nose Y ratio 中位數，
  test7 為 0.36207203

Ground Truth：

| 時段 | 狀態 | 內容 |
| --- | --- | --- |
| 0–44.6 秒 | `normal` | 正坐、低頭閱讀、回正 |
| 44.6–58.8 秒 | `sleeping` | 反覆低下／抬起的打瞌睡 |
| 58.8–89.5 秒 | `normal` | 回正、第二段低頭閱讀、回正 |
| 89.5–106.7 秒 | `sleeping` | 低頭後維持的靜態睡覺 |
| 106.7 秒～結束 | `normal` | 回正 |

3-of-5 二元 frame 結果：

| Accuracy | Sleeping Precision | Sleeping Recall | Sleeping F1 | TP／FP／FN／TN |
| ---: | ---: | ---: | ---: | ---: |
| 88.9% | 99.3% | 60.6% | 75.3% | 571／4／371／2440 |

分段結果：

| 區段 | 幀數 | 預測 sleeping 幀 | 命中率／誤報率 |
| --- | ---: | ---: | ---: |
| 13.1–37.9 秒閱讀 | 744 | 0 | 0.0% 誤報 |
| 44.6–58.8 秒打瞌睡 | 426 | 89 | 20.9% 命中 |
| 65.7–87.5 秒閱讀 | 654 | 0 | 0.0% 誤報 |
| 89.5–106.7 秒靜態睡覺 | 516 | 482 | 93.4% 命中 |
| 106.7 秒後 normal | 186 | 4 | 2.2% 誤報 |

兩個 sleeping 事件都配對成功，沒有漏失事件；第一段 onset delay 2435 ms，第二段 1138 ms，
平均 1786.5 ms。第一段被輸出成 4 個短 sleeping 片段，事件比較的一對一配對因此把另外 3 段
計為 false-positive events；這些片段都在 sleeping 真值內，不是閱讀誤報。兩段閱讀合計
1398 幀均未輸出 sleeping。

| 區段 | Face 可見率 | Pitch 中位數 | Pitch 速度 P90 | Nose Y 中位數／IQR |
| --- | ---: | ---: | ---: | ---: |
| 第一段閱讀 | 100.0% | 34.03° | 4.88°/s | 0.470／0.010 |
| 打瞌睡 | 82.2% | 17.63° | 44.94°/s | 0.430／0.090 |
| 第二段閱讀 | 100.0% | 33.47° | 7.54°/s | 0.470／0.010 |
| 靜態睡覺 | 42.2% | 42.99° | 30.46°/s | 0.540／0.040 |

獨立驗證**部分支持**候選：固定深度規則能可靠避免把閱讀判成睡覺，也能穩定抓到頭維持
低位的睡覺；但只在打瞌睡頭降到夠深時才觸發，該段召回僅 20.9%，且第一段延遲超過預設
2 秒標準 435 ms，所以未完全通過。test7 明確支持加入動態訊號：打瞌睡的 Pitch 速度 P90
為兩段閱讀的約 6–9 倍。下一版應保留深度規則偵測靜態睡覺，再以頭部快速下降／回升作
打瞌睡觸發並加入 latch；動態規則若用 test7 開發，必須再用新的 test8 做獨立驗證。

### V2 開發：test7 動態觸發＋睡眠 latch

- 日期：2026-09-03
- 定位：test7 自本節起改為開發資料；本節數字不是獨立驗證結果
- 目的：補足固定深度規則只能在打瞌睡頭降得夠深時短暫觸發的問題，同時維持閱讀低誤報
- 原始 CSV 未修改；正式 App 未修改；候選只輸出新的後處理 CSV

test7 的順序特徵是：打瞌睡開始時，使用者先在接近正坐的位置持續閉眼，接著頭部下降；
閱讀開始時則是頭先下降，低頭後 EAR 才因眼鏡與角度出現大量 `raw_eye_closed`。因此動態路徑
不把 Pitch 速度單獨當成睡眠，而使用以下時序：

```text
baseline = 前 5 個合格 Pose 樣本的 nose_y_ratio 中位數

靜態路徑（沿用 test6，未調整）：
  nose_y_ratio - baseline >= 0.13

動態路徑（test7 開發）：
  接近正坐：nose drop <= 0.05 且 abs(pitch) <= 15°
  接近正坐時 raw_eye_closed 持續 >= 300 ms → arm
  arm 後 2 秒內 nose drop >= 0.06 且 abs(pitch) >= 15° → sleeping latch
  若 arm 後維持正坐且睜眼 >= 500 ms → 取消 arm
  latch 後，臉與頭部穩定回正 >= 2500 ms → normal
  最終 raw_state = 靜態路徑 OR 動態 latch
  再套因果 3-of-5
```

test7 開發集結果：

| 策略 | Accuracy | Precision | Recall | F1 | 打瞌睡命中 | 靜態睡覺命中 | 閱讀誤報 | 狀態切換 |
| --- | ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| 固定深度＋3-of-5 | 88.9% | 99.3% | 60.6% | 75.3% | 20.9% | 93.4% | 0 / 1398 | 10 |
| 動態＋固定深度＋3-of-5 | 95.7% | 93.9% | 90.6% | 92.2% | 87.1% | 93.4% | 0 / 1398 | 4 |

動態版 TP／FP／FN／TN 為 853／55／89／2389。兩段 sleeping 均配對成功，無額外預測事件；
onset delay 分別為 1835 ms 與 1138 ms，平均 1486.5 ms。第一段 latch 在 60.503 秒解除，
比 58.8 秒真值晚約 1.7 秒；最後回正後另有 4 幀 3-of-5 尾端，因此 Precision 從固定深度版
99.3% 降為 93.9%，但打瞌睡不再被切成 4 個短片段。

回歸檢查：

| 影片 | 檢查結果 |
| --- | --- |
| test5 | 1799 幀全部輸出 normal；879 幀低頭閱讀 0 誤報，dynamic trigger 0 次 |
| test6 | 兩段低頭閱讀 0 誤報；低頭睡覺輸出 37.335–57.036 秒，onset delay 1035 ms |
| test7 | 兩段閱讀 0 誤報；兩段睡眠均命中，動態 trigger 1 次 |

test6 的正坐持續閉眼仍不會被本候選判成 sleeping，因為本策略刻意處理「低頭閱讀 vs
低頭睡覺／打瞌睡」，不重新定義產品層 `fatigue` 與 `sleeping`。目前資料也沒有「低頭看書
→ 抬頭查電腦 → 再低頭看書」的困難負樣本，因此不能推論該情境不會誤報。下一步凍結上述
所有常數，以包含工作切換、打瞌睡與靜態睡覺的 test8 做獨立驗證；看 test8 前不再調參。

### V2 獨立驗證：test8 工作切換困難負樣本

- 日期：2026-09-03
- 影片：`assets/test8.mp4`，研究者自行拍攝
- Run ID：`test8_1788445572747073`
- 原始資料：3860 幀，PTS 0–128643 ms，46 欄；解碼器記錄 1 個 3 ms dropped sample
- 驗證規則：test7 凍結的動態觸發＋固定深度＋因果 3-of-5；test8 結果出來前未調參
- baseline Nose Y ratio：0.36136005
- 標記規則：30.1–31.55 秒短暫閉眼仍算 normal；122.2 秒後嚴格標 normal，另以 3 秒
  release grace 驗收抬頭過程

Ground Truth：

| 時段 | 狀態 | 內容 |
| --- | --- | --- |
| 0–52.8 秒 | `normal` | baseline、讀書查資料、短暫閉眼、閉眼後閱讀、回正 |
| 52.8–67.9 秒 | `sleeping` | 第一段打瞌睡／睡眠 |
| 67.9–106.4 秒 | `normal` | 恢復、第二段讀書查資料 |
| 106.4–122.2 秒 | `sleeping` | 第二段睡眠 |
| 122.2 秒～結束 | `normal` | 抬頭後恢復 |

3-of-5 二元 frame 結果：

| Accuracy | Sleeping Precision | Sleeping Recall | Sleeping F1 | TP／FP／FN／TN |
| ---: | ---: | ---: | ---: | ---: |
| 74.7% | 48.6% | 93.5% | 64.0% | 867／916／60／2017 |

分段結果：

| 區段 | 幀數 | 預測 sleeping 幀 | 結果 |
| --- | ---: | ---: | --- |
| 8.3–29.8 秒讀書查資料 | 645 | 0 | 通過 |
| 30.1–31.55 秒短暫閉眼 | 44 | 0 | 通過 |
| 31.55–43.2 秒閉眼後閱讀 | 349 | 0 | 通過 |
| 52.8–67.9 秒第一段睡眠 | 453 | 417（92.1%） | 通過 |
| 67.9–77.7 秒恢復 | 294 | 87（29.6%） | 70.806 秒解除，2.906 秒內，通過 grace |
| 77.7–106.4 秒第二段讀書查資料 | 861 | 826（95.9%） | **嚴重誤報，失敗** |
| 106.4–122.2 秒第二段睡眠 | 474 | 450（94.9%） | 通過 |
| 122.2 秒後 normal | 194 | 3（1.5%） | 122.310 秒解除，通過 grace |

預測事件為 54.006–70.806 秒 sleeping、78.008–105.541 秒 sleeping、
107.208–122.310 秒 sleeping。兩段真睡眠都配對成功，onset delay 分別為 1206 ms 與
808 ms，平均 1007 ms；但中間多出 1 個長達約 27.5 秒的完整誤報事件，狀態切換共 6 次。

test8 揭露動態 arm 的明確缺陷。第二段閱讀開始約 77.5 秒時，頭已開始下降但仍落在
`nose drop <= 0.05`、`abs(pitch) <= 15°` 的「接近正坐」範圍；角度造成的
`raw_eye_closed` 連續到足以 arm，隨後 78.0 秒 nose drop 約 0.094、pitch 約 29.7°，正好
滿足下降觸發，於是閱讀切換被 latch 成 sleeping。也就是說「閉眼證據發生在低頭前」仍不夠，
還必須證明閉眼證據建立時頭部是穩定的，而不是已在下降途中。

本次獨立驗證**不通過**。固定規則通過短暫閉眼後閱讀、兩段真睡眠、延遲與 release grace，
但最重要的工作切換負樣本失敗，不能整合進正式 App。test8 自此可轉為下一版開發資料；若加入
「arm 前頭部穩定／低速度」條件或其他修正，必須再以新的 test9 做獨立驗證，不能重新把
test8 分數當成驗證成效。相同後處理重跑兩次逐位元相同。

---

### V2 開發：test8 穩定閉眼 arm 與舊片回歸

- 日期：2026-09-04
- 定位：test7＋test8 為開發資料；test5、test6、test7 用於回歸，不重新稱為獨立驗證
- 範圍：只修改 `tool/vision_lab_reclassify_sleep_context.dart`，不修改正式 App、原始 CSV、
  人工 GT、EAR 計算或固定深度路徑；沒有重跑 MediaPipe
- 模式：預設 `stable-arm`；`legacy-arm` 保留上一輪規則，重跑四支影片均與歷史衍生 CSV
  逐位元相同

開發觀察：test8 在 77.575–77.875 秒誤觸發前的閉眼證據段，pitch 範圍約 13.65°，
nose ratio 範圍約 0.03425；test7 真打瞌睡起始閉眼段的前約 0.3 秒分別為 0.59°／0.00388，
test8 第一段真睡眠則為 0.27°／0.00848。這支持先測試「閉眼證據成立時頭部穩定」的限制，
但不是對實際閉眼原因的影像證明，也不表示所有閱讀和睡眠都能以此區分。

本輪只試下列固定候選，沒有全面掃描參數：

```text
保留原接近正坐、連續閉眼 300 ms、下降觸發、2 秒 arm 有效期與 2.5 秒解除規則。
在尚未 arm 的閉眼證據累積期間，加上：
  max(pitch) - min(pitch) <= 3°
  max(nose_y_ratio) - min(nose_y_ratio) <= 0.015
超過任一範圍：從當前幀重新累積完整 300 ms。
睜眼或 face/pose 無效：中斷閉眼證據累積。
arm 成立後不再用穩定限制阻止下降；原 0.13 固定深度路徑不變。
最後仍套相同因果 3-of-5。
```

使用的原始 Run ID：`test5_1788334581949564`、`test6_1788355603679529`、
`test7_1788425425933008`、`test8_1788445572747073`。本輪衍生檔以
`frame_features_stable_arm_<runId>.csv`／`frame_features_legacy_arm_<runId>.csv` 命名，
事件與總體指標由既有 `tool/vision_lab_compare.dart` 產出；Precision／Recall／F1 與分段
計數另由逐幀 GT 交叉核算，並確認逐幀投票結果與事件 CSV 完全一致。

#### test5～test7 回歸

| 影片／區段 | 舊版 | 新版 | 結果 |
| --- | ---: | ---: | --- |
| test5 閱讀 23.5–52.8 秒 | 0 / 879 誤報 | 0 / 879 誤報 | 不變 |
| test6 兩段閱讀 | 0 / 582 誤報 | 0 / 582 誤報 | 不變 |
| test6 低頭睡覺 | 591 / 651 命中（90.8%） | 591 / 651 命中（90.8%） | 不變 |
| test7 兩段閱讀 | 0 / 1398 誤報 | 0 / 1398 誤報 | 不變 |
| test7 打瞌睡 | 371 / 426 命中（87.1%） | 371 / 426 命中（87.1%） | 不變 |
| test7 靜態睡覺 | 482 / 516 命中（93.4%） | 482 / 516 命中（93.4%） | 不變 |

三片原始輸出共 7449 幀，新舊 `raw_state` 變動 **0 幀**，3-of-5 結果與事件邊界也不變。
test6 低頭睡覺延遲維持 1035 ms；test7 兩段延遲維持 1835／1138 ms，平均 1486.5 ms。
test7 整片 Accuracy 95.7%、Precision 93.9%、Recall 90.6%、F1 92.2% 均不變。

範圍限制：test6 正坐閉眼 14.9–25.9 秒的 330 幀，新舊皆未判 sleeping，並非這次新增的
退步，但也不能忽略。依既有 GT 計算 test6 整片，兩版皆 Accuracy 82.8%、Precision 100%、
Recall 60.2%、F1 75.2%，漏失 1 個正坐閉眼事件。test5 也只有閱讀負樣本回歸，不能據
「全片輸出 normal」宣稱閉眼辨識成功。test1～test4 的原始 CSV 缺少此規則所需的 Pose 鼻子
座標，未納入；此次不是所有既有影片或所有姿勢的完整回歸。

#### test8 開發集結果

下表為整片、嚴格 GT 邊界、sleeping 為正類；沒有扣掉恢復寬限期的誤報幀。

| 指標 | legacy-arm＋3-of-5 | stable-arm＋3-of-5 |
| --- | ---: | ---: |
| Accuracy | 74.7% | 95.5% |
| Precision | 48.6% | 88.5% |
| Recall | 93.5% | 93.5% |
| F1 | 64.0% | 90.9% |
| TP／FP／FN／TN | 867／916／60／2017 | 867／113／60／2820 |
| 誤報事件 | 1 | 3 |
| 漏失事件 | 0 | 0 |
| 狀態切換 | 6 | 10 |
| 平均 onset delay | 1007 ms | 1007 ms |

| 區段 | 舊 sleeping 幀 | 新 sleeping 幀 |
| --- | ---: | ---: |
| 8.3–29.8 秒讀書查資料 | 0 / 645 | 0 / 645 |
| 30.1–31.55 秒短暫閉眼 | 0 / 44 | 0 / 44 |
| 31.55–43.2 秒閱讀 | 0 / 349 | 0 / 349 |
| 52.8–67.9 秒真睡眠 | 417 / 453 | 417 / 453 |
| 67.9–77.7 秒恢復 | 87 / 294 | 87 / 294 |
| 77.7–106.4 秒讀書查資料 | 826 / 861（95.9%） | 23 / 861（2.7%） |
| 106.4–122.2 秒真睡眠 | 450 / 474 | 450 / 474 |
| 122.2 秒後恢復 | 3 / 194 | 3 / 194 |

新規則移除閱讀段的長時間 dynamic latch；仍殘留固定深度路徑造成的 3 個短 sleeping 事件：
78.608–78.908 秒、89.875–90.075 秒、90.275–90.541 秒，總長 0.766 秒。
舊規則該段為 78.008–105.541 秒，長 27.533 秒。因此**誤報時間大幅縮短，但誤報事件數與
切換數反而增加**，不能說所有指標都改善。23 個閱讀誤報幀之外，另 90 個誤報幀都在睡眠後
恢復段。真睡眠事件起訖仍為 54.006–70.806 秒與 107.208–122.310 秒，偵測延遲和解除
時點完全不變。本輪不為清掉剩餘短誤報繼續調整深度或投票條件。

驗證：四片的非 `raw_state` 欄位均逐欄保留；四片新版後處理各重跑兩次逐位元相同。
Vision 測試共 55 項通過（既有 45＋新增 10），涵蓋穩定／移動閉眼、鼻子移動、短閉眼、
追蹤消失、重新穩定、arm 過期／取消、深度路徑及 latch 解除。

結論：此次開發降低 test8 的長時間閱讀誤報，且 test5～test7 未見逐幀回歸退步；仍有短
誤報，也未解決所有閉眼／睡眠型態。新參數自此凍結，等 test9 獨立驗證，暫不整合正式 App。

#### test9 拍攝協議（看結果前凍結）

使用同樣鏡頭位置、眼鏡和照明先測新影片泛化，約 2～3 分鐘即可。不需要精準跟下面秒數，
錄完依實際影片標記；不要刻意配合「先閉眼穩定 300 ms」或某個低頭角度。

| 建議順序 | 約時長 | 標記與動作 |
| --- | --- | --- |
| 正常正坐 | 10 秒 | normal，眼睛自然睜開，建立 baseline |
| 讀書／看電腦查資料 | 25 秒 | normal，自然來回 3～4 次，混合快慢低頭 |
| 短閉眼後繼續閱讀 | 15 秒 | 正坐休息閉眼約 1～2 秒，睜眼後很快低頭閱讀；本次 sleeping 二元驗證仍標 normal |
| 模擬打瞌睡 | 20 秒 | sleeping，自然下垂、抬一下、再低下，不用刻意安排閉眼和頭動的順序 |
| 回正恢復 | 10 秒 | normal，恢復日常坐姿 |
| 第二輪讀書／查資料 | 25 秒 | normal，再次來回 3～4 次，特別測睡眠後的工作切換 |
| 從閱讀中進入低頭睡眠 | 20 秒 | sleeping，已低頭時閉眼，頭可緩慢下降後維持；不必先抬頭來配合規則 |
| 回正恢復 | 10 秒 | normal，保留足夠尾段觀察解除 |

驗收預先定義：兩輪工作切換各 sleeping 誤報幀率 <= 5%，且單一連續誤報事件 <= 1 秒；
短暫休息負樣本同樣檢查，不混進真睡眠。兩段睡眠各命中 >= 80%，各 onset delay <= 2 秒；
恢復後解除 <= 3 秒。整片仍報嚴格 GT 的 Accuracy／Precision／Recall／F1、誤報事件、
漏失、切換與延遲，解除寬限只作額外驗收，不從主指標刪除。

錄影是模擬行為，`sleeping` 真值代表人工標記的模擬睡眠，不是生理睡眠診斷。保留眼睛是否
閉合與頭部開始下降的可辨時間供失敗分析；若邊界不確定，用備註標記，不依預測事後移動 GT。
test9 看結果前不調參；任何未過條件如實記錄，若再拿 test9 調整，就需新的獨立影片。

### V2 獨立驗證：test9 穩定閉眼 arm

- 日期：2026-09-04
- 影片：`assets/test9.mp4`，研究者自行拍攝，720×1280、H.264，容器長度約 100.637 秒
- Run ID：`test9_1788511300828684`
- 規則：沿用 test7＋test8 開發並凍結的 `stable-arm`，沒有用 test9 調參；以
  `legacy-arm` 作同份 CSV 的對照，正式 App 不變
- 影片長度依使用者時間限制縮短，沿用先前驗收條件，動作時間以實拍後標記為準
- GT 在執行後處理前建立；原始語意與 binary sleeping 評估 GT 分開保存

| 時段 | 使用者標記 | sleeping 二元評估 |
| --- | --- | --- |
| 0–7.4 秒 | 正常 | normal |
| 7.4–23.2 秒 | 讀書查資料 | normal |
| 23.2–25.3 秒 | 閉眼 | normal（本輪短暫休息負樣本，不表示眼睛睜開） |
| 25.3–36.1 秒 | 讀書 | normal |
| 36.1–50 秒 | 打瞌睡，含幾幀抬頭 | sleeping |
| 50–59 秒 | 正常 | normal |
| 59–77.3 秒 | 讀書 | normal |
| 77.3–92.6 秒 | 睡眠 | sleeping |
| 92.6 秒～結束 | 正常 | normal |

第一段打瞌睡的抬頭幀沒有更精確人工起訖，先依使用者指示整段 sleeping；短暫 normal 可作
行為上的合理例外，但主指標仍以嚴格 GT 計分，不能以預測或 pitch 自動改 GT、扣掉漏失。

抽取共 3018 幀、46 欄，PTS 0–100603 ms。解碼器記錄來源 3019 個 sample，其中
31 ms 的 1 個 sample 未輸出。使用原有原生解碼與特徵計算，沒有主動抽樣或跳幀加速。
此次只重跑後處理做逐位元一致性驗證，未把整段 MediaPipe 抽取重跑第二次。

#### 整片結果

sleeping 為正類，嚴格 GT 邊界；下表的「不另投票」仍含 sleep-context 內部的 arm/latch，
不是零記憶 FrameClassifier。平均 delay 的負值受到閱讀誤報跨入真睡眠影響，見下文。

| 規則 | 最後投票 | Accuracy | Precision | Recall | F1 | 誤報事件 | 漏失 | 切換 | 平均 delay |
| --- | --- | ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| legacy-arm | 不另投票 | 69.6% | 48.8% | 95.8% | 64.6% | 15 | 0 | 34 | -6016.0 ms |
| legacy-arm | 3-of-5 | 70.1% | 49.2% | 97.1% | 65.4% | 8 | 0 | 20 | -5949.5 ms |
| stable-arm | 不另投票 | 73.0% | 51.9% | 95.8% | 67.3% | 15 | 0 | 34 | -6016.0 ms |
| stable-arm | 3-of-5 | 73.6% | 52.4% | 97.1% | 68.1% | 8 | 0 | 20 | -5949.5 ms |

stable-arm＋3-of-5：TP 851、FP 773、FN 25、TN 1369。legacy-arm＋3-of-5：
TP 851、FP 877、FN 25、TN 1265。新版少 104 個閱讀誤報幀，真睡眠命中不變，但仍有
773 個閱讀誤報幀，不能只根據高 Recall 宣稱成功。

#### 分段結果（stable-arm＋3-of-5）

| 區段 | sleeping 幀／總幀 | 命中或誤報率 | 最長連續誤報 | 驗收 |
| --- | ---: | ---: | ---: | --- |
| 0–7.4 秒正坐 | 0 / 222 | 0% 誤報 | 0 | 正常 |
| 7.4–23.2 秒讀書查資料 | 143 / 473 | 30.2% 誤報 | 3.700 秒 | 失敗 |
| 23.2–25.3 秒短暫閉眼 | 0 / 63 | 0% 誤報 | 0 | 通過 |
| 25.3–36.1 秒閉眼後閱讀 | 282 / 324 | 87.0% 誤報 | 9.134 秒 | 失敗 |
| 36.1–50 秒打瞌睡 | 393 / 417 | 94.2% 命中 | 不適用 | 命中率通過，起始受提前誤報污染 |
| 50–59 秒恢復 | 0 / 270 | 0% 誤報 | 0 | 無解除拖延 |
| 59–77.3 秒閱讀 | 348 / 549 | 63.4% 誤報 | 4.500 秒 | 失敗 |
| 77.3–92.6 秒低頭睡眠 | 458 / 459 | 99.8% 命中 | 不適用 | 命中率通過，起始受提前誤報污染 |
| 92.6 秒後恢復 | 0 / 241 | 0% 誤報 | 0 | 無解除拖延 |

三段閱讀合計 1346 幀，773 幀誤報（57.4%），按 PTS 計約 25.766 秒。第一段打瞌睡中
normal 為 24 幀，落在 48.335–48.935 秒與 49.801–50.000 秒，總長約 0.799 秒。
使用者允許短暫抬頭判 normal；此處保留嚴格 FN，不自行認定這些幀都是抬頭，但無論是否
給予該例外，都不改變本次主要失敗是閱讀誤報的結論。

事件一對一配對把 26.966–48.335 秒的預測配給 36.1 秒開始的真睡眠，delay = -9134 ms；
74.535–92.570 秒的預測配給 77.3 秒開始的真睡眠，delay = -2765 ms。這是「閱讀時已誤報，
後來碰巧延伸到睡眠」，不是提早偵測成功。平均 -5949.5 ms 保留原計算值，但不應用來
宣稱起始延遲驗收通過。8 個未配對事件中有 1 個是在第一段真睡眠內的碎片，其餘 7 個
完全在 normal 內；另有閱讀誤報被包進已配對事件，因此事件誤報數本身無法描述全部誤報時間。

#### 原因與結論

新版 `dynamic_triggers = 0`、`dynamic_sleeping_frames = 0`，所有 1616 個未投票 sleeping
幀都來自原封不動的固定深度路徑；舊版 dynamic trigger 為 1 次。穩定 arm 限制有減少
第一段查資料的動態誤報（247 → 143 幀），但無法攔截另一條獨立的深度路徑。

本片 baseline nose ratio = 0.41717463。下表是 Pose 合格幀相對 baseline 的下降量：

| 區段 | 下降量 P10 | 中位數 | P90 |
| --- | ---: | ---: | ---: |
| 第一段讀書查資料 | -0.0058 | 0.1200 | 0.1380 |
| 短閉眼後閱讀 | 0.1179 | 0.1567 | 0.1625 |
| 打瞌睡 | 0.1487 | 0.2512 | 0.2980 |
| 第二段閱讀 | 0.0004 | 0.1460 | 0.1642 |
| 低頭睡眠 | 0.2485 | 0.2848 | 0.2959 |

正常閱讀的相對下降量已常超過固定 0.13，所以不需要任何閉眼或動態觸發，就會被判為睡眠。
這些觀察支持「固定低頭深度不足以跨影片區分閱讀／睡眠」，不代表可以直接把 0.13 換成
test9 上更漂亮的數字。此片兩種姿勢的深度有差異，也不等於新門檻能泛化。

**本次獨立驗證不通過。** 睡眠片段覆蓋率高、短暫休息與回正沒有誤報，但閱讀誤報遠超
預設 5%／1 秒限制，且不能據高睡眠命中宣稱閉眼辨識成功，因為本輪 sleeping 全靠深度。
未改任何規則或門檻，不把 test9 改列為已調參資料，也不整合進正式 App。

原始 CSV、語意 GT、sleeping GT、新舊後處理 CSV、事件及比較指標均留在 `vision_lab_out/`。
已核對衍生 CSV 除 `raw_state` 外逐欄不變，逐幀投票與事件 CSV 一致；stable-arm 後處理
跑兩次逐位元相同。test9 Lab APK 建置成功，完整 pubspec 資產清單已還原（僅新增 test9），
本次啟動的背景模擬器已關閉。這輪不重新宣稱完成一次新的正式 App 行為測試。

### V2 開發：test9 肩膀相對位置證據與回歸取捨

- 日期：2026-09-04
- test9 從本節起是開發資料；上節原始獨立驗證失敗結果保留，本節不能再稱為獨立驗證
- 範圍：Lab 新增可選 `shoulder-evidence` 模式，正式 App 不修改；預設仍為 `stable-arm`
- 使用 test5～test9 既有 Pose CSV；未重跑 MediaPipe、未修改原始資料或人工 GT
- 目的：檢查低頭深度改為候選後，加入頭相對雙肩位置是否能降低閱讀誤報，並量化漏失代價

#### 候選規則

```text
depth_candidate = pose_detected 且 nose_y_ratio - baseline >= 0.13
shoulder_width = hypot(left_shoulder_x - right_shoulder_x,
                       left_shoulder_y - right_shoulder_y)
gap = ((left_shoulder_y + right_shoulder_y) / 2 - pose_nose_y) / shoulder_width

幾何可用：座標皆有限，image_height > 0，肩寬 >= 0.05 × image_height，
          鼻子／左右肩 visibility 各在 [0.5, 1] 內
static_sleeping = depth_candidate 且 幾何可用 且 gap <= 0.15
最終 sleeping = static_sleeping 或原 stable-arm 的 dynamic latch
最後再套同一因果 3-of-5
```

gap 是鼻子到雙肩平均高度的有號垂直距離，以投影肩寬正規化；負值表示鼻子已低於雙肩平均
高度。不需要看到眼睛或臀部，但「看不到臉」本身絕不是睡眠證據。這是新增的姿勢條件，
仍與原鼻子深度相關，不是獨立的閉眼或生理睡眠證據。

本輪只測這一組保守候選，未掃描門檻，也未因舊片退步而加入逐片特例。開發前的分布已顯示
重疊：test9 短閉眼後閱讀的 gap 中位數約 0.199，test6 真睡眠約 0.189，難以用一條通用
gap 門檻同時保住兩者。0.15 是此次開發候選值，不是文獻或獨立驗證支持的通用門檻。

#### 整片比較（sleeping 正類，3-of-5）

| 影片 | 方法 | Accuracy | Precision | Recall | F1 | TP／FP／FN／TN |
| --- | --- | ---: | ---: | ---: | ---: | --- |
| test6 | stable-arm | 82.8% | 100.0% | 60.2% | 75.2% | 591／0／390／1283 |
| test6 | shoulder-evidence | 58.0% | 100.0% | 3.2% | 6.1% | 31／0／950／1283 |
| test7 | stable-arm | 95.7% | 93.9% | 90.6% | 92.2% | 853／55／89／2389 |
| test7 | shoulder-evidence | 89.3% | 92.5% | 67.0% | 77.7% | 631／51／311／2393 |
| test8 | stable-arm | 95.5% | 88.5% | 93.5% | 90.9% | 867／113／60／2820 |
| test8 | shoulder-evidence | 90.5% | 88.1% | 69.8% | 77.9% | 647／87／280／2846 |
| test9 | stable-arm | 73.6% | 52.4% | 97.1% | 68.1% | 851／773／25／1369 |
| test9 | shoulder-evidence | 94.6% | 100.0% | 81.3% | 89.7% | 712／0／164／2142 |

test6 整片 GT 包含正坐閉眼事件，新舊均漏失它，因此需同時看下面的低頭睡眠分段；test5
僅作閱讀負樣本回歸，不用全片輸出 normal 宣稱閉眼偵測成功。

| 區段 | stable-arm | shoulder-evidence |
| --- | ---: | ---: |
| test5 閱讀誤報 | 0 / 879 | 0 / 879 |
| test6 兩段閱讀誤報 | 0 / 582 | 0 / 582 |
| test6 低頭睡眠命中 | 591 / 651（90.8%） | 31 / 651（4.8%） |
| test7 兩段閱讀誤報 | 0 / 1398 | 0 / 1398 |
| test7 打瞌睡命中 | 371 / 426（87.1%） | 371 / 426（87.1%） |
| test7 靜態睡眠命中 | 482 / 516（93.4%） | 260 / 516（50.4%） |
| test8 第二段查資料誤報 | 23 / 861（2.7%） | 0 / 861 |
| test8 第一段睡眠命中 | 417 / 453（92.1%） | 417 / 453（92.1%） |
| test8 第二段睡眠命中 | 450 / 474（94.9%） | 230 / 474（48.5%） |
| test9 三段閱讀誤報 | 773 / 1346（57.4%） | 0 / 1346 |
| test9 打瞌睡命中 | 393 / 417（94.2%） | 279 / 417（66.9%） |
| test9 低頭睡眠命中 | 458 / 459（99.8%） | 433 / 459（94.3%） |

test8 第一段查資料與閉眼後閱讀也維持 0 誤報。新候選在五片中沒有新增 sleeping 幀，只移除
舊預測中的一部分；因此壓低誤報並不意外，關鍵是它同時造成大量漏失。

#### 事件、延遲與碎片化

下表箭頭方向皆為 stable-arm → shoulder-evidence，沿用事件一對一配對規則。

| 影片 | 誤報事件 | 漏失事件 | 狀態切換 | 平均 onset delay |
| --- | ---: | ---: | ---: | ---: |
| test6 | 0 → 0 | 1 → 1 | 2 → 2 | 1035 → 19070 ms |
| test7 | 0 → 2 | 0 → 0 | 4 → 8 | 1486.5 → 1720 ms |
| test8 | 3 → 17 | 0 → 0 | 10 → 38 | 1007 → 1107 ms |
| test9 | 8 → 4 | 0 → 0 | 20 → 12 | -5949.5 → 467 ms |

test9 新候選首次觸發為 36.766 與 77.568 秒，相對 GT 延遲為 666／268 ms，不再因閱讀
誤報提前跨入睡眠而產生負延遲。但第一段睡眠被切成 4 段、第二段切成 2 段；4 個額外事件
全在真睡眠內，屬一對一配對下的碎片誤報，而非正常閱讀誤報。即使 test9 整片 Recall 81.3%，
第一段仍只有 66.9%，不能用整體分數掩蓋分段失敗。

test6 低頭睡眠只剩 55.370–56.403 秒被抓到；test8 第二段睡眠被切成 18 段，造成 17 個
未配對事件。短片段重疊到真值便會算配對成功，所以「事件漏失沒增加」不表示辨識沒退步。

#### 實作、驗證與決定

- CLI 明確指定 `shoulder-evidence` 才使用新條件，舊預設與 `legacy-arm` 都保留。
- 輸出 `unconfirmed_depth_frames` 診斷計數：test5～test9 分別為 0、560、214、230、908。
  代表深度達標但沒有靜態幾何支持、也沒有 dynamic latch 的幀。此類在二元 CSV 中不發
  sleeping 警示，並不表示證明清醒；不從指標分母排除，GT 為 sleeping 時仍完整計 FN。
- 新模式要求鼻子與雙肩必要欄位；欄位缺少明確報錯。無效值／低可信度不當作有效證據。
- 五片全部確認非 `raw_state` 欄位逐欄保留、逐幀投票與事件 CSV 一致。新模式各跑兩次
  逐位元相同；stable-arm 和 legacy-arm 各回放五片，均與本輪前保存的輸出逐位元相同。
- Vision 64 項測試通過（本輪新增 9 項），靜態分析通過；正式 App 程式未修改。
- 原始資料與 GT 仍使用前述 run ID；本輪衍生檔為 `frame_features_shoulder_evidence_<runId>.csv`，
  比較檔與事件檔沿用 `shoulder_evidence_<runId>`。所有 CSV 留在 Git 忽略的 `vision_lab_out/`。

**決定：不升級為預設，不整合正式 App。** 這版能降低深閱讀誤報，但對較淺睡眠與動態片段
造成不可接受的回歸。預設保留舊模式是為了可重現與避免悄悄改行為，不表示舊模式已可靠。
目前資料證明的是姿勢特徵重疊及誤報／漏失取捨，尚未解決閱讀與睡眠區分；不能宣稱只要再調
一個通用深度／肩膀距離門檻就會成功。下一輪需另議可驗證的補充證據或明確校正協議，
而不是先換預設、或急著把新片當成成功驗收。

## V2 探索分析：低頭情境下融合眼部 openness 可分性

### 問題與資料角色

- 日期：2026-09-04
- Branch：`codex/vision-lab-v2-pose-context`
- 使用既有的 `frame_features_test6_1788355603679529.csv`、
  `frame_features_test7_1788425425933008.csv`、
  `frame_features_test8_1788445572747073.csv`、
  `frame_features_test9_1788511300828684.csv`（46 欄，含原生端融合 blendshape 與
  landmark 後的 `eye_open_l`／`eye_open_r`）；MediaPipe 未重跑，正式 App 未修改。
- 補做「V2 探索分析：移除固定閉眼端點」一節提出但未執行的診斷：「把原生端融合
  openness 寫入 CSV，檢查閱讀／閉眼是否仍有可分性」。
- 本節分析使用**一次性離線 script**（未納入 `tool/`，未經 Vision Lab CLI，只讀取
  既有 CSV），不是正式 Vision Lab 實驗，不能視為新的獨立驗證或既有規則的替代方案。

### 區段定義

閱讀與低頭睡覺的時間區段，取自本文件先前各影片小節記錄的時間；沒有另外建立逐段
ground truth CSV，因此區段邊界精確度依賴原記錄，未重新用影片覆核：

| 影片 | 閱讀區段（ms） | 低頭睡覺區段（ms） |
| --- | --- | --- |
| test6 | 27000–36300、58000–68100 | 36300–58000 |
| test7 | 13100–37900、65700–87500 | 44600–58800、89500–106700 |
| test8 | 8300–29800、31550–43200、77700–106400 | 52800–67900、106400–122200 |
| test9 | 7400–23200、25300–36100、59000–77300 | 36100–50000、77300–92600 |

test6 正坐持續閉眼區段（14900–25900 ms）不計入本節，因為該段已知可由 EAR 高度
可分，不是本節要檢查的低頭情境。

### 方法

只取 `face_detected` 為真的幀（才有有效 `eye_open_l`／`eye_open_r`），計算
`eye_open = (eye_open_l + eye_open_r) / 2`，比較閱讀與低頭睡覺兩類的分布，並在
`[0.10, 0.85]` 之間掃描候選門檻，計算「閱讀誤報率」（`eye_open < 門檻` 的閱讀幀
比例）與「睡覺命中率」（`eye_open < 門檻` 的睡覺幀比例）。

### 結果

各影片與四片合併：

| 影片 | 類別 | 幀數 | 臉可見率 | eye_open P10／P50／P90 |
| --- | --- | ---: | ---: | --- |
| test6 | 閱讀 | 582 | 100.0% | 0.142／0.241／0.274 |
| test6 | 低頭睡覺 | 651 | 33.3% | 0.057／0.233／0.302 |
| test7 | 閱讀 | 1398 | 100.0% | 0.000／0.131／0.334 |
| test7 | 低頭睡覺 | 942 | 60.3% | 0.000／0.290／0.660 |
| test8 | 閱讀 | 1855 | 100.0% | 0.048／0.354／0.481 |
| test8 | 低頭睡覺 | 927 | 32.1% | 0.000／0.220／0.526 |
| test9 | 閱讀 | 1346 | 99.6% | 0.053／0.167／0.270 |
| test9 | 低頭睡覺 | 876 | 17.5% | 0.063／0.159／0.381 |
| 四片合併 | 閱讀 | 5181 | 99.9% | 0.023／0.215／0.401 |
| 四片合併 | 低頭睡覺 | 3396 | 36.4% | 0.000／0.239／0.570 |

門檻掃描（四片合併，僅臉可見幀，`readN=5175`、`sleepN=1236`）：

| 門檻 | 閱讀誤報率 | 睡覺命中率 |
| ---: | ---: | ---: |
| 0.10 | 20.6% | 28.7% |
| 0.20 | 46.4% | 43.7% |
| 0.30 | 69.6% | 64.2% |
| 0.40 | 89.9% | 81.7% |
| 0.50 | 96.6% | 87.5% |
| 0.60 | 97.4% | 91.0% |
| 0.70 | 99.9% | 97.1% |
| 0.80 | 100.0% | 100.0% |

### 觀察與決策

- 合併後低頭睡覺的 eye_open 中位數（0.239）略高於閱讀（0.215），兩個分布幾乎完全
  重疊；沒有一個門檻能同時壓低閱讀誤報並保住睡覺命中——兩者幾乎同步隨門檻升降。
- 低頭睡覺整體臉可見率僅 36.4%（test9 甚至只有 17.5%），代表多數低頭睡覺幀連
  `eye_open` 都拿不到值，眼睛路線在頭已低下後能覆蓋的範圍本來就有限。
- 本節確認：即使改用原生端融合 blendshape 與 landmark 的 `eye_open`，而不是 raw
  EAR，在「低頭閱讀 vs 低頭睡覺」這個切分上仍沒有可用的可分性。這回答了「V2 探索
  分析：移除固定閉眼端點」一節留下的問題，結論是否定的。
- 先前已測過的固定深度、動態 arm、肩膀相對位置，各自在某些影片上有效、換一支片就
  出現不可接受的取捨（見前兩節）；本節確認連眼部融合訊號本身也不能補足。這比較支持
  「目前這組單視角視覺特徵，在頭低下之後對閱讀／睡覺的區分已到上限」，而不是還沒調
  到對的門檻或對的特徵組合。
- 本節不是正式 Vision Lab 實驗：區段邊界取自既有文字記錄而非獨立標註 CSV，script
  未納入 `tool/`，也未經過逐位元可重現性檢查。若要正式收錄為決策依據，應改用獨立
  ground truth CSV 重跑並經過相同的可重現性驗收。
- 建議：暫停在現有 EAR／深度／肩膀／眼部融合特徵上繼續調參找「低頭閱讀 vs 低頭
  睡覺」的規則。若要再嘗試，下一個真正沒測過的變因是相機擺放角度（頭低下時能否
  維持眼睛可見），而不是再換一種姿勢幾何或再融合另一個既有欄位。

---

## 新實驗模板

複製本節並附加到文件尾端；不要覆寫舊實驗。

### 實驗名稱：`<名稱>`

- 日期：`YYYY-MM-DD`
- 目的／假設：
- Git branch／commit：
- FrameClassifier／策略版本：
- 投票設定：
- Ground Truth 標記者與規則：

#### 影片來源

| 影片 | 來源／拍攝方式 | 拍攝裝置 | 內容 | Run ID | 幀數 | PTS 範圍 |
| --- | --- | --- | --- | --- | ---: | ---: |
|  |  |  |  |  |  |  |

#### 結果

| 影片 | 策略 | Frame accuracy | Precision／Recall／F1 | 誤報 | 漏失 | 狀態切換 | 平均延遲 |
| --- | --- | ---: | --- | ---: | ---: | ---: | ---: |
|  |  |  |  |  |  |  |  |

#### 觀察與決策

- 觀察：
- 已知限制：
- 是否支持假設：
- 下一步／停止條件：
